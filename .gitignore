# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / credenciais (nunca commitar)
.env
.env.*
!.env.example
# .env.production é exceção de propósito: não tem credencial nenhuma, só força
# NODE_ENV=production no build. Precisa ser versionado, senão a proteção vale
# só nesta máquina (ver o próprio arquivo).
!.env.production

# Artefatos de teste de navegador (Playwright MCP)
.playwright-mcp/
/login-*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI temp
supabase/.temp/

# Spec OpenAPI da uazapi (600 KB, baixado do painel deles) — consulta local,
# nao versionado. O que importa dele esta resumido em docs/whatsapp.md.
docs/uazapi-openapi-spec.yaml
