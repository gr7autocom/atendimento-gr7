# Inbox (filas, conversa, ações) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development ou superpowers:executing-plans. Steps usam checkbox (`- [ ]`).

**Goal:** Entregar a Inbox operacional: filas (Meus / Pendentes / Potenciais), thread da conversa, painel do contato e as ações Assumir, Responder, Transferir e Finalizar, com um simulador para criar chamados de exemplo sem uazapi.

**Architecture:** Camada de dados própria (`useInbox`) com TanStack Query fazendo joins (`contato`, `departamento`, `responsavel`); a UI é uma página de 3 colunas com seleção de chamado por estado local. Uma migration aditiva libera INSERT de `atendimentos` para quem tem `atendimento.responder`, o que destrava tanto o simulador quanto o registro manual de chamado.

**Tech Stack:** React 19, TanStack Query, Supabase JS, Tailwind, lucide-react.

## Global Constraints

- **Visual cru é esperado** (ADR-10). Só coerência: dark, `text-[#ffffff]`, cores sólidas, ícones `lucide-react`, sem emoji.
- **RLS manda:** a fila só mostra o que `atende_departamento`/`responsavel_id`/`e_plantonista` permitem. O usuário de teste já está vinculado a SUPORTE GERAL.
- **Migration aditiva** no repo do painel, aplicada por `db push`. Nada de `ALTER`/`DROP`.
- **Copy** sem travessão como conector.
- Status válidos: `triagem` | `na_fila` | `em_atendimento` | `finalizado`. Regras de fluxo em [docs/bot.md](../../bot.md).

## File Structure

- `painel-implantacao-v2/supabase/migrations/<ts>_atendimento_insert_policy.sql` — Create
- `src/lib/useInbox.ts` — Create: listagem de atendimentos, mensagens e ações
- `src/pages/Inbox.tsx` — Modify: de placeholder para a inbox real
- `src/components/inbox/ListaChamados.tsx` — Create: coluna 1 (abas + lista)
- `src/components/inbox/Conversa.tsx` — Create: coluna 2 (thread + ações + envio)
- `src/components/inbox/PainelContato.tsx` — Create: coluna 3
- `src/components/inbox/SimuladorChamado.tsx` — Create: cria chamado de exemplo
- Test: `src/lib/useInbox.test.tsx`

---

## Task 1: Migration — permitir criar atendimento

**Files:** Create `painel-implantacao-v2/supabase/migrations/20260724100000_atendimento_insert_policy.sql`

- [ ] **Step 1: Escrever a migration**

```sql
-- Atendimento GR7: permite que quem opera atendimento crie um chamado.
-- Necessario para o registro manual (ex.: contato que chegou por telefone) e
-- para o simulador usado enquanto a integracao com a uazapi nao existe.
DROP POLICY IF EXISTS atendimentos_insert ON public.atendimentos;
CREATE POLICY atendimentos_insert ON public.atendimentos FOR INSERT TO authenticated
  WITH CHECK (can('atendimento.responder'));
```

- [ ] **Step 2: Aplicar** — em `painel-implantacao-v2`: `npx supabase db push`. Expected: aplica só essa migration.
- [ ] **Step 3: Commit no repo do painel** — `git commit -m "feat(atendimento): policy de insert de atendimento"`

---

## Task 2: Camada de dados da Inbox

**Files:** Create `src/lib/useInbox.ts`, Test `src/lib/useInbox.test.tsx`

**Interfaces:**
- Produces:
  - `AtendimentoLista = { id, protocolo, status, departamento_id, responsavel_id, ultima_mensagem_em, contato: { id, nome, nome_whatsapp, telefone, cliente_id } | null, departamento: { nome } | null }`
  - `useAtendimentos()` → `UseQueryResult<AtendimentoLista[]>` (ordenado por `ultima_mensagem_em` desc)
  - `useMensagens(atendimentoId | null)` → `UseQueryResult<Mensagem[]>`, `Mensagem = { id, direcao, origem, corpo, created_at, remetente_usuario_id }`
  - `useAcoesAtendimento()` → `{ assumir, responder, finalizar, transferir }` (mutations que invalidam `atendimentos` e `mensagens`)

- [ ] **Step 1: Teste** (`useInbox.test.tsx`) — a listagem devolve os atendimentos com o contato

```tsx
import { describe, it, expect, vi } from 'vitest'
import { renderHook, waitFor } from '@testing-library/react'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import type { ReactNode } from 'react'

const order = vi.fn().mockResolvedValue({
  data: [{ id: 'a1', protocolo: 1, status: 'na_fila', contato: { id: 'c1', nome: 'João', telefone: '+5511999' } }],
  error: null,
})
const select = vi.fn(() => ({ order }))
vi.mock('./supabase', () => ({ supabase: { from: vi.fn(() => ({ select })) } }))

import { useAtendimentos } from './useInbox'

function wrapper({ children }: { children: ReactNode }) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return <QueryClientProvider client={qc}>{children}</QueryClientProvider>
}

describe('useAtendimentos', () => {
  it('lista atendimentos com o contato', async () => {
    const { result } = renderHook(() => useAtendimentos(), { wrapper })
    await waitFor(() => expect(result.current.isSuccess).toBe(true))
    expect(result.current.data?.[0].contato?.nome).toBe('João')
  })
})
```

- [ ] **Step 2: Rodar (falha)** — `npm test -- src/lib/useInbox.test.tsx`

- [ ] **Step 3: Implementar `src/lib/useInbox.ts`**

```ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { supabase } from './supabase'

export type ContatoResumo = {
  id: string
  nome: string | null
  nome_whatsapp: string | null
  telefone: string
  cliente_id: string | null
}

export type AtendimentoLista = {
  id: string
  protocolo: number
  status: 'triagem' | 'na_fila' | 'em_atendimento' | 'finalizado'
  departamento_id: string | null
  responsavel_id: string | null
  ultima_mensagem_em: string
  contato: ContatoResumo | null
  departamento: { nome: string } | null
}

export type Mensagem = {
  id: string
  direcao: 'entrada' | 'saida'
  origem: 'cliente' | 'atendente' | 'bot'
  corpo: string | null
  created_at: string
  remetente_usuario_id: string | null
}

const SELECT_ATENDIMENTO =
  '*, contato:contatos(id, nome, nome_whatsapp, telefone, cliente_id), departamento:departamentos(nome)'

export function useAtendimentos() {
  return useQuery({
    queryKey: ['atendimentos'],
    queryFn: async () => {
      const { data, error } = await supabase
        .from('atendimentos')
        .select(SELECT_ATENDIMENTO)
        .order('ultima_mensagem_em', { ascending: false })
      if (error) throw error
      return (data ?? []) as unknown as AtendimentoLista[]
    },
    refetchInterval: 10000,
  })
}

export function useMensagens(atendimentoId: string | null) {
  return useQuery({
    queryKey: ['atendimento_mensagens', atendimentoId],
    enabled: !!atendimentoId,
    queryFn: async () => {
      const { data, error } = await supabase
        .from('atendimento_mensagens')
        .select('*')
        .eq('atendimento_id', atendimentoId)
        .order('created_at')
      if (error) throw error
      return (data ?? []) as unknown as Mensagem[]
    },
    refetchInterval: 10000,
  })
}

export function useAcoesAtendimento() {
  const qc = useQueryClient()
  const invalidar = () => {
    qc.invalidateQueries({ queryKey: ['atendimentos'] })
    qc.invalidateQueries({ queryKey: ['atendimento_mensagens'] })
  }

  const assumir = useMutation({
    mutationFn: async ({ id, usuarioId }: { id: string; usuarioId: string }) => {
      const { error } = await supabase
        .from('atendimentos')
        .update({ responsavel_id: usuarioId, status: 'em_atendimento', assumido_em: new Date().toISOString() } as never)
        .eq('id', id)
      if (error) throw error
    },
    onSuccess: invalidar,
  })

  /** Responder assume o chamado se ninguém pegou (regra do fluxo, ver bot.md). */
  const responder = useMutation({
    mutationFn: async ({
      atendimentoId, corpo, usuarioId, precisaAssumir,
    }: { atendimentoId: string; corpo: string; usuarioId: string; precisaAssumir: boolean }) => {
      const { error } = await supabase.from('atendimento_mensagens').insert({
        atendimento_id: atendimentoId, direcao: 'saida', origem: 'atendente',
        corpo, remetente_usuario_id: usuarioId, status: 'enviado',
      } as never)
      if (error) throw error
      const patch: Record<string, unknown> = { ultima_mensagem_em: new Date().toISOString() }
      if (precisaAssumir) {
        patch.responsavel_id = usuarioId
        patch.status = 'em_atendimento'
        patch.assumido_em = new Date().toISOString()
      }
      const { error: err2 } = await supabase.from('atendimentos').update(patch as never).eq('id', atendimentoId)
      if (err2) throw err2
    },
    onSuccess: invalidar,
  })

  const finalizar = useMutation({
    mutationFn: async ({ id, motivoId }: { id: string; motivoId: string | null }) => {
      const { error } = await supabase
        .from('atendimentos')
        .update({
          status: 'finalizado', motivo_id: motivoId,
          finalizado_em: new Date().toISOString(), encerrado_por: 'atendente',
        } as never)
        .eq('id', id)
      if (error) throw error
    },
    onSuccess: invalidar,
  })

  const transferir = useMutation({
    mutationFn: async ({
      id, paraDepartamentoId, paraUsuarioId, de,
    }: {
      id: string
      paraDepartamentoId: string | null
      paraUsuarioId: string | null
      de: { departamentoId: string | null; usuarioId: string | null; porUsuarioId: string }
    }) => {
      const patch: Record<string, unknown> = {}
      if (paraUsuarioId) {
        patch.responsavel_id = paraUsuarioId
        patch.status = 'em_atendimento'
        if (paraDepartamentoId) patch.departamento_id = paraDepartamentoId
      } else if (paraDepartamentoId) {
        patch.departamento_id = paraDepartamentoId
        patch.responsavel_id = null
        patch.status = 'na_fila'
      }
      const { error } = await supabase.from('atendimentos').update(patch as never).eq('id', id)
      if (error) throw error
      const { error: err2 } = await supabase.from('atendimento_transferencias').insert({
        atendimento_id: id,
        de_departamento_id: de.departamentoId,
        para_departamento_id: paraDepartamentoId,
        de_usuario_id: de.usuarioId,
        para_usuario_id: paraUsuarioId,
        transferido_por: de.porUsuarioId,
      } as never)
      if (err2) throw err2
    },
    onSuccess: invalidar,
  })

  return { assumir, responder, finalizar, transferir }
}
```

- [ ] **Step 4: Rodar teste + build** → PASS
- [ ] **Step 5: Commit** — `git commit -m "feat: camada de dados da inbox"`

---

## Task 3: Simulador de chamado

**Files:** Create `src/components/inbox/SimuladorChamado.tsx`

Enquanto não há uazapi, este botão cria um chamado de exemplo: contato (telefone aleatório), atendimento em `na_fila` no departamento escolhido e uma mensagem de entrada.

- [ ] **Step 1: Implementar**

```tsx
import { useState } from 'react'
import { MessageSquarePlus } from 'lucide-react'
import { useQueryClient } from '@tanstack/react-query'
import { supabase } from '../../lib/supabase'
import { useCrud } from '../../lib/useCrud'

type Departamento = { id: string; nome: string; ativo: boolean }

export function SimuladorChamado() {
  const departamentos = useCrud<Departamento>('departamentos')
  const qc = useQueryClient()
  const [criando, setCriando] = useState(false)
  const [erro, setErro] = useState<string | null>(null)

  async function simular() {
    setCriando(true)
    setErro(null)
    try {
      const dep = (departamentos.lista.data ?? [])[0]
      const sufixo = String(Math.floor(Math.random() * 90000) + 10000)
      const telefone = `+55119${sufixo}0000`.slice(0, 14)

      const { data: contato, error: e1 } = await supabase
        .from('contatos')
        .insert({ telefone, nome_whatsapp: `Cliente ${sufixo}` } as never)
        .select('id')
        .single()
      if (e1) throw e1

      const { data: at, error: e2 } = await supabase
        .from('atendimentos')
        .insert({
          contato_id: (contato as { id: string }).id,
          departamento_id: dep?.id ?? null,
          status: 'na_fila',
          canal: 'whatsapp',
        } as never)
        .select('id')
        .single()
      if (e2) throw e2

      const { error: e3 } = await supabase.from('atendimento_mensagens').insert({
        atendimento_id: (at as { id: string }).id,
        direcao: 'entrada',
        origem: 'cliente',
        corpo: 'Olá, preciso de ajuda com o sistema.',
      } as never)
      if (e3) throw e3

      qc.invalidateQueries({ queryKey: ['atendimentos'] })
    } catch (e) {
      setErro(e instanceof Error ? e.message : 'Falha ao simular o chamado.')
    } finally {
      setCriando(false)
    }
  }

  return (
    <div className="flex flex-col gap-1">
      <button
        onClick={simular}
        disabled={criando}
        title="Cria um chamado de exemplo enquanto a integração com o WhatsApp não existe"
        className="flex items-center gap-1 rounded bg-[#ffffff14] text-[#ffffff] text-sm px-3 py-1.5 disabled:opacity-50"
      >
        <MessageSquarePlus size={16} /> {criando ? 'Criando…' : 'Simular chamado'}
      </button>
      {erro && <span className="text-red-400 text-xs">{erro}</span>}
    </div>
  )
}
```

- [ ] **Step 2: Build** → ok
- [ ] **Step 3: Commit** — `git commit -m "feat: simulador de chamado (sem uazapi)"`

---

## Task 4: UI da Inbox (3 colunas)

**Files:** Create `src/components/inbox/ListaChamados.tsx`, `Conversa.tsx`, `PainelContato.tsx`; Modify `src/pages/Inbox.tsx`

**Interfaces:** Consumes `useAtendimentos`, `useMensagens`, `useAcoesAtendimento`, `useUsuarioAtual`, `useCrud('departamentos')`, `useCrud('atendimento_motivos')`.

Filas: **Meus** (`responsavel_id = eu` e não finalizado) · **Pendentes** (`status = na_fila`) · **Potenciais** (`contato.cliente_id` nulo e não finalizado).

- [ ] **Step 1: `ListaChamados.tsx`** — abas + lista, recebe `atendimentos`, `selecionadoId`, `onSelecionar`, `usuarioId`.
- [ ] **Step 2: `Conversa.tsx`** — thread + campo de resposta + botões Assumir / Transferir / Finalizar (com motivo).
- [ ] **Step 3: `PainelContato.tsx`** — nome editável, telefone, empresa vinculada, protocolo, status.
- [ ] **Step 4: `Inbox.tsx`** — junta as 3 colunas, mantém `selecionadoId` e o `SimuladorChamado` no topo.
- [ ] **Step 5: Testes + build** → verde
- [ ] **Step 6: Commit** — `git commit -m "feat: inbox com filas, conversa e acoes"`

---

## Task 5: Validação no navegador

- [ ] Logar como admin (já vinculado a SUPORTE GERAL), abrir `/inbox`.
- [ ] Clicar **Simular chamado**: aparece em **Pendentes**.
- [ ] Selecionar, **Assumir**: sai de Pendentes e entra em **Meus**, status Em atendimento.
- [ ] **Responder**: a mensagem aparece na thread.
- [ ] **Transferir** para outro departamento: sai da minha fila (efeito correto da RLS).
- [ ] **Finalizar** com motivo: sai das filas ativas.
- [ ] Limpar os chamados de teste ao final.
- [ ] `npm test` verde, `npm run build` ok.

## Self-Review

- Cobertura: filas, conversa, ações e painel do contato conforme [docs/telas.md](../../telas.md). Tags e histórico do contato ficam para um refinamento posterior, junto da etapa de design.
- A policy de INSERT é aditiva e defensável (registro manual de chamado), não só para o simulador.
- Bot real (motor de triagem/menu) continua fora: entra com a uazapi.
