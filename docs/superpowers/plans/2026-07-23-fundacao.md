# Fundação (Scaffold) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deixar o app do Atendimento rodando com tema dark do painel, autenticação compartilhada (mesmo Supabase) e navegação entre as duas áreas (`/inbox` e `/admin`), pronto para receber as telas de banco/admin/inbox dos próximos planos.

**Architecture:** React 19 + Vite + TypeScript. Tailwind v4 sem config, tokens dark **copiados** do painel. Supabase client apontando para o mesmo projeto do painel; a auth reusa as tabelas `usuarios`/`permissoes` já existentes. Roteamento com react-router-dom v7, estado de servidor com TanStack Query. O código **não importa** do painel — os arquivos reaproveitados são **copiados**.

**Tech Stack:** React 19, Vite 8, TypeScript, TailwindCSS v4 (`@tailwindcss/vite`), `@supabase/supabase-js`, `react-router-dom` v7, `@tanstack/react-query`, `zod`, `lucide-react`, `clsx`, `tailwind-merge`. Testes: Vitest + Testing Library (jsdom).

## Global Constraints

- **Isolamento do painel:** nunca `import` de `../painel-implantacao-v2`. Reaproveitar = **copiar** o arquivo para `src/`.
- **Tema dark idêntico ao painel**, tokens copiados de `painel-implantacao-v2/src/design-tokens.css`. Gotcha herdado: o token inverte a escala de cinza do Tailwind (`text-white` vira cinza escuro); usar `text-[#ffffff]` literal (ver [docs/design.md](../../design.md)).
- **Regras de UI globais:** cores sólidas (sem gradiente); sem emoji na interface (ícones só `lucide-react`); responsivo (foco desktop, sem rolagem horizontal no mobile).
- **Credenciais:** só via env (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`). Nunca commitar `.env` (já está no `.gitignore`).
- **Copy/PT:** textos de UI sem travessão como conector; identificadores de domínio em português.

---

## Pré-requisito (manual, antes da Task 1)

Criar `.env` na raiz do projeto com as credenciais do **mesmo** Supabase do painel (copiar os valores do `.env` do painel, não commitar):

```
VITE_SUPABASE_URL=https://ghweohedmmmkufqhxdzn.supabase.co
VITE_SUPABASE_ANON_KEY=<copiar do .env do painel>
```

Criar também `.env.example` (esse pode commitar) com os nomes e valores vazios.

---

## File Structure

- `package.json` — Modify: dependências novas
- `vite.config.ts` — Modify: plugin Tailwind + config de teste
- `src/index.css` — Modify: import do Tailwind + tokens
- `src/design-tokens.css` — Create (copiado do painel)
- `src/test-setup.ts` — Create: jest-dom
- `src/lib/utils.ts` — Create: `cn` (copiado do painel, enxuto)
- `src/lib/supabase.ts` — Create: client (copiado do painel)
- `src/lib/types.ts` — Create: tipos de auth enxutos
- `src/lib/auth.tsx` — Create: AuthProvider (adaptado do painel)
- `src/lib/permissoes.ts` — Create: `usePermissao` (`can`/`isAdmin`, enxuto)
- `src/lib/acoes.ts` — Create: catálogo de `AcaoId` do atendimento
- `src/components/ProtectedRoute.tsx` — Create
- `src/components/Layout.tsx` — Create: sidebar + navegação
- `src/pages/Login.tsx` — Create
- `src/pages/Inbox.tsx` — Create: placeholder
- `src/pages/Admin.tsx` — Create: placeholder + sub-abas
- `src/App.tsx` — Modify: rotas + providers
- `src/main.tsx` — mantém

---

## Task 1: Instalar dependências

**Files:** Modify `package.json` (via npm)

- [ ] **Step 1: Instalar dependências de runtime**

```bash
npm install @supabase/supabase-js@^2.103.3 react-router-dom@^7.14.1 @tanstack/react-query@^5.62.0 zod@^3.24.1 lucide-react@^1.8.0 clsx@^2.1.1 tailwind-merge@^3.5.0 tailwindcss@^4.2.2 @tailwindcss/vite@^4.2.2
```

- [ ] **Step 2: Instalar dependências de desenvolvimento (testes)**

```bash
npm install -D vitest@^4.1.4 jsdom@^29.0.2 @testing-library/react@^16.3.2 @testing-library/jest-dom@^6.9.1 @testing-library/user-event@^14.6.1 @testing-library/dom@^10.4.1
```

- [ ] **Step 3: Adicionar script de teste ao `package.json`**

No bloco `"scripts"`, adicionar:

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: Verificar que instalou sem erro**

Run: `npm ls @supabase/supabase-js react-router-dom @tanstack/react-query tailwindcss`
Expected: mostra as versões instaladas, sem `UNMET DEPENDENCY`.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: instala dependencias da fundacao (supabase, router, query, tailwind, testes)"
```

---

## Task 2: Tailwind v4 + tokens dark + config de teste

**Files:**
- Modify: `vite.config.ts`
- Create: `src/design-tokens.css`, `src/test-setup.ts`
- Modify: `src/index.css`

**Interfaces:**
- Produces: build com Tailwind ativo; `npm test` roda Vitest com jsdom + jest-dom.

- [ ] **Step 1: Copiar os tokens do painel**

```bash
cp ../painel-implantacao-v2/src/design-tokens.css src/design-tokens.css
```

- [ ] **Step 2: Reescrever `vite.config.ts`** (plugin Tailwind + Vitest, sem PWA)

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
    // env dummy p/ os testes: sem isso, src/lib/supabase.ts lança no import
    // (ele exige VITE_SUPABASE_URL/ANON_KEY). Valores falsos bastam: sem sessão
    // salva, getSession() resolve como null sem bater na rede.
    env: {
      VITE_SUPABASE_URL: 'http://localhost:54321',
      VITE_SUPABASE_ANON_KEY: 'test-anon-key',
    },
  },
})
```

- [ ] **Step 3: Criar `src/test-setup.ts`**

```ts
import '@testing-library/jest-dom'
```

- [ ] **Step 4: Reescrever `src/index.css`** (Tailwind + tokens)

```css
@import 'tailwindcss';
@import './design-tokens.css';
```

- [ ] **Step 5: Rodar o build para validar Tailwind**

Run: `npm run build`
Expected: build conclui sem erro (gera `dist/`).

- [ ] **Step 6: Commit**

```bash
git add vite.config.ts src/design-tokens.css src/test-setup.ts src/index.css
git commit -m "feat: configura tailwind v4, tokens dark e vitest"
```

---

## Task 3: Helpers de lib (`cn` + Supabase client)

**Files:**
- Create: `src/lib/utils.ts`, `src/lib/supabase.ts`
- Test: `src/lib/utils.test.ts`

**Interfaces:**
- Produces: `cn(...inputs)` → string; `supabase` (SupabaseClient).

- [ ] **Step 1: Escrever o teste de `cn`**

`src/lib/utils.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { cn } from './utils'

describe('cn', () => {
  it('junta classes e resolve conflito do tailwind', () => {
    expect(cn('p-2', 'p-4')).toBe('p-4')
    expect(cn('text-sm', false && 'hidden', 'font-bold')).toBe('text-sm font-bold')
  })
})
```

- [ ] **Step 2: Rodar o teste (deve falhar — módulo não existe)**

Run: `npm test -- src/lib/utils.test.ts`
Expected: FAIL ("Cannot find module './utils'").

- [ ] **Step 3: Criar `src/lib/utils.ts`**

```ts
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

- [ ] **Step 4: Criar `src/lib/supabase.ts`**

```ts
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL as string | undefined
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY as string | undefined

if (!supabaseUrl || !supabaseAnonKey) {
  const msg =
    'Variáveis VITE_SUPABASE_URL e/ou VITE_SUPABASE_ANON_KEY não definidas. Confira o arquivo .env.'
  console.error(msg)
  throw new Error(msg)
}

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

- [ ] **Step 5: Rodar o teste (deve passar)**

Run: `npm test -- src/lib/utils.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/lib/utils.ts src/lib/supabase.ts src/lib/utils.test.ts
git commit -m "feat: helper cn e cliente supabase"
```

---

## Task 4: Auth compartilhada + permissões

**Files:**
- Create: `src/lib/types.ts`, `src/lib/acoes.ts`, `src/lib/auth.tsx`, `src/lib/permissoes.ts`
- Test: `src/lib/auth.test.tsx`

**Interfaces:**
- Consumes: `supabase` (Task 3).
- Produces: `AuthProvider`, `useAuth()` → `{ status, usuario, signIn, signOut, recarregar }`, `useUsuarioAtual()`, `usePermissao()` → `{ can, isAdmin, slug }`. `AcaoId` inclui as ações do atendimento.

- [ ] **Step 1: Criar `src/lib/types.ts`** (tipos de auth enxutos)

```ts
export type Permissao = {
  id: string
  nome: string
  slug: string | null
  cor: string | null
  capacidades: string[] | null
}

export type Usuario = {
  id: string
  auth_user_id: string | null
  nome: string
  email: string
  ativo: boolean
  status: 'ativo' | 'pendente' | 'inativo' | null
}

export type UsuarioAutenticado = Usuario & {
  permissao: Pick<Permissao, 'id' | 'nome' | 'slug' | 'cor' | 'capacidades'> | null
}
```

- [ ] **Step 2: Criar `src/lib/acoes.ts`** (catálogo de ações do atendimento)

```ts
export const ACOES_ATENDIMENTO = [
  'atendimento.assumir',
  'atendimento.responder',
  'atendimento.finalizar',
  'atendimento.transferir',
  'atendimento.config',
] as const

export type AcaoId = (typeof ACOES_ATENDIMENTO)[number]
```

- [ ] **Step 3: Criar `src/lib/auth.tsx`** (adaptado do painel, enxuto)

```tsx
import { createContext, useContext, useEffect, useRef, useState, type ReactNode } from 'react'
import type { Session } from '@supabase/supabase-js'
import { supabase } from './supabase'
import type { UsuarioAutenticado } from './types'

type Status = 'loading' | 'authenticated' | 'unauthenticated' | 'unauthorized'

type AuthContextValue = {
  status: Status
  usuario: UsuarioAutenticado | null
  unauthorizedReason: string | null
  signIn: (email: string, password: string) => Promise<{ error: string | null }>
  signOut: () => Promise<void>
  recarregar: () => Promise<void>
}

const AuthContext = createContext<AuthContextValue | null>(null)
const SELECT_USUARIO = '*, permissao:permissoes(id, nome, slug, cor, capacidades)'

async function resolverUsuario(
  session: Session
): Promise<{ kind: 'ok'; usuario: UsuarioAutenticado } | { kind: 'error'; reason: string }> {
  const { data } = await supabase
    .from('usuarios')
    .select(SELECT_USUARIO)
    .eq('auth_user_id', session.user.id)
    .maybeSingle()
  if (!data) {
    return { kind: 'error', reason: 'Não há perfil ativo com este e-mail no sistema. Contate o administrador.' }
  }
  const u = data as unknown as UsuarioAutenticado
  if (!u.ativo || u.status === 'inativo') {
    return { kind: 'error', reason: 'Sua conta está desativada. Contate o administrador.' }
  }
  return { kind: 'ok', usuario: u }
}

export function AuthProvider({ children }: { children: ReactNode }) {
  const [status, setStatus] = useState<Status>('loading')
  const [usuario, setUsuario] = useState<UsuarioAutenticado | null>(null)
  const [unauthorizedReason, setUnauthorizedReason] = useState<string | null>(null)
  const callIdRef = useRef(0)

  async function processarSession(session: Session | null) {
    const callId = ++callIdRef.current
    if (!session) {
      if (callId !== callIdRef.current) return
      setUsuario(null)
      setUnauthorizedReason(null)
      setStatus('unauthenticated')
      return
    }
    const res = await resolverUsuario(session)
    if (callId !== callIdRef.current) return
    if (res.kind === 'ok') {
      setUsuario(res.usuario)
      setUnauthorizedReason(null)
      setStatus('authenticated')
    } else {
      setUsuario(null)
      setUnauthorizedReason(res.reason)
      setStatus('unauthorized')
    }
  }

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => processarSession(data.session))
    const {
      data: { subscription },
    } = supabase.auth.onAuthStateChange((event, session) => {
      if (event === 'INITIAL_SESSION') return
      processarSession(session)
    })
    return () => subscription.unsubscribe()
  }, [])

  async function signIn(email: string, password: string) {
    const { error } = await supabase.auth.signInWithPassword({ email: email.trim(), password })
    return { error: error?.message ?? null }
  }
  async function signOut() {
    await supabase.auth.signOut()
  }
  async function recarregar() {
    const { data } = await supabase.auth.getSession()
    await processarSession(data.session)
  }

  return (
    <AuthContext.Provider value={{ status, usuario, unauthorizedReason, signIn, signOut, recarregar }}>
      {children}
    </AuthContext.Provider>
  )
}

export function useAuth(): AuthContextValue {
  const ctx = useContext(AuthContext)
  if (!ctx) throw new Error('useAuth deve ser usado dentro de AuthProvider')
  return ctx
}

export function useUsuarioAtual(): UsuarioAutenticado | null {
  return useAuth().usuario
}
```

- [ ] **Step 4: Criar `src/lib/permissoes.ts`**

```ts
import { useAuth } from './auth'
import type { AcaoId } from './acoes'

export function usePermissao() {
  const { usuario } = useAuth()
  const slug = usuario?.permissao?.slug ?? null
  const capacidades = (usuario?.permissao?.capacidades ?? []) as AcaoId[]

  function can(acao: AcaoId): boolean {
    return capacidades.includes(acao)
  }
  return { slug, isAdmin: slug === 'admin', can }
}
```

- [ ] **Step 5: Escrever o teste de `useAuth` fora do provider**

`src/lib/auth.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest'
import { renderHook } from '@testing-library/react'
import { useAuth } from './auth'

describe('useAuth', () => {
  it('lança erro fora do AuthProvider', () => {
    expect(() => renderHook(() => useAuth())).toThrow('useAuth deve ser usado dentro de AuthProvider')
  })
})
```

- [ ] **Step 6: Rodar o teste**

Run: `npm test -- src/lib/auth.test.tsx`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/lib/types.ts src/lib/acoes.ts src/lib/auth.tsx src/lib/permissoes.ts src/lib/auth.test.tsx
git commit -m "feat: auth compartilhada e permissoes do atendimento"
```

---

## Task 5: Rotas, guarda de acesso e providers

**Files:**
- Create: `src/components/ProtectedRoute.tsx`
- Modify: `src/App.tsx`
- Test: `src/components/ProtectedRoute.test.tsx`

**Interfaces:**
- Consumes: `useAuth` (Task 4).
- Produces: `<ProtectedRoute requireAdmin?>` que redireciona para `/login` quando `unauthenticated`. `App` monta `AuthProvider` + `QueryClientProvider` + `BrowserRouter`.

- [ ] **Step 1: Criar `src/components/ProtectedRoute.tsx`**

```tsx
import { Navigate } from 'react-router-dom'
import type { ReactNode } from 'react'
import { useAuth } from '../lib/auth'
import { usePermissao } from '../lib/permissoes'

export function ProtectedRoute({ children, requireAdmin = false }: { children: ReactNode; requireAdmin?: boolean }) {
  const { status } = useAuth()
  const { isAdmin } = usePermissao()

  if (status === 'loading') return <div className="p-6 text-[#ffffff]">Carregando…</div>
  if (status !== 'authenticated') return <Navigate to="/login" replace />
  if (requireAdmin && !isAdmin) return <Navigate to="/inbox" replace />
  return <>{children}</>
}
```

- [ ] **Step 2: Reescrever `src/App.tsx`** (providers + rotas)

```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { AuthProvider } from './lib/auth'
import { ProtectedRoute } from './components/ProtectedRoute'
import { Layout } from './components/Layout'
import { Login } from './pages/Login'
import { Inbox } from './pages/Inbox'
import { Admin } from './pages/Admin'

const queryClient = new QueryClient()

export default function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <AuthProvider>
        <BrowserRouter>
          <Routes>
            <Route path="/login" element={<Login />} />
            <Route
              path="/inbox"
              element={
                <ProtectedRoute>
                  <Layout>
                    <Inbox />
                  </Layout>
                </ProtectedRoute>
              }
            />
            <Route
              path="/admin/*"
              element={
                <ProtectedRoute requireAdmin>
                  <Layout>
                    <Admin />
                  </Layout>
                </ProtectedRoute>
              }
            />
            <Route path="*" element={<Navigate to="/inbox" replace />} />
          </Routes>
        </BrowserRouter>
      </AuthProvider>
    </QueryClientProvider>
  )
}
```

- [ ] **Step 3: Escrever o teste do ProtectedRoute** (redireciona sem sessão)

`src/components/ProtectedRoute.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter, Routes, Route } from 'react-router-dom'
import { ProtectedRoute } from './ProtectedRoute'
import { AuthProvider } from '../lib/auth'

describe('ProtectedRoute', () => {
  it('redireciona para /login quando não autenticado', async () => {
    render(
      <AuthProvider>
        <MemoryRouter initialEntries={['/protegida']}>
          <Routes>
            <Route path="/login" element={<div>tela de login</div>} />
            <Route
              path="/protegida"
              element={
                <ProtectedRoute>
                  <div>conteudo secreto</div>
                </ProtectedRoute>
              }
            />
          </Routes>
        </MemoryRouter>
      </AuthProvider>
    )
    expect(await screen.findByText('tela de login')).toBeInTheDocument()
  })
})
```

Nota: sem sessão, `resolverUsuario` não é chamado; o status sai de `loading` para `unauthenticated` após o `getSession()` inicial, então o teste usa `findByText` (assíncrono).

- [ ] **Step 4: Rodar o teste**

Run: `npm test -- src/components/ProtectedRoute.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/ProtectedRoute.tsx src/components/ProtectedRoute.test.tsx src/App.tsx
git commit -m "feat: rotas, guarda de acesso e providers"
```

---

## Task 6: Layout (sidebar) + telas placeholder

**Files:**
- Create: `src/components/Layout.tsx`, `src/pages/Login.tsx`, `src/pages/Inbox.tsx`, `src/pages/Admin.tsx`
- Test: `src/pages/Login.test.tsx`

**Interfaces:**
- Consumes: `useAuth`, `usePermissao`.
- Produces: `<Layout>` com navegação Inbox/Admin (Admin só aparece para `isAdmin`); `<Login>`, `<Inbox>`, `<Admin>` renderizáveis.

- [ ] **Step 1: Criar `src/pages/Login.tsx`**

```tsx
import { useState, type FormEvent } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../lib/auth'

export function Login() {
  const { signIn } = useAuth()
  const navigate = useNavigate()
  const [email, setEmail] = useState('')
  const [senha, setSenha] = useState('')
  const [erro, setErro] = useState<string | null>(null)
  const [enviando, setEnviando] = useState(false)

  async function onSubmit(e: FormEvent) {
    e.preventDefault()
    setEnviando(true)
    setErro(null)
    const { error } = await signIn(email, senha)
    setEnviando(false)
    if (error) {
      setErro('Não foi possível entrar. Confira e-mail e senha.')
      return
    }
    navigate('/inbox', { replace: true })
  }

  return (
    <div className="min-h-screen flex items-center justify-center p-4">
      <form onSubmit={onSubmit} className="w-full max-w-sm flex flex-col gap-3">
        <h1 className="text-[#ffffff] text-xl font-bold">GR7 Atendimento</h1>
        <input
          type="email"
          placeholder="E-mail"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          className="rounded px-3 py-2 bg-[#ffffff1a] text-[#ffffff]"
          required
        />
        <input
          type="password"
          placeholder="Senha"
          value={senha}
          onChange={(e) => setSenha(e.target.value)}
          className="rounded px-3 py-2 bg-[#ffffff1a] text-[#ffffff]"
          required
        />
        {erro && <p className="text-red-400 text-sm">{erro}</p>}
        <button
          type="submit"
          disabled={enviando}
          className="rounded px-3 py-2 bg-[#0078d4] text-[#ffffff] font-medium disabled:opacity-60"
        >
          {enviando ? 'Entrando…' : 'Entrar'}
        </button>
      </form>
    </div>
  )
}
```

- [ ] **Step 2: Criar `src/components/Layout.tsx`**

```tsx
import type { ReactNode } from 'react'
import { NavLink } from 'react-router-dom'
import { MessageSquare, Settings, LogOut } from 'lucide-react'
import { useAuth } from '../lib/auth'
import { usePermissao } from '../lib/permissoes'
import { cn } from '../lib/utils'

export function Layout({ children }: { children: ReactNode }) {
  const { signOut, usuario } = useAuth()
  const { isAdmin } = usePermissao()

  const linkCls = ({ isActive }: { isActive: boolean }) =>
    cn(
      'flex items-center gap-2 px-3 py-2 rounded text-[#ffffff] text-sm',
      isActive ? 'bg-[#ffffff26]' : 'hover:bg-[#ffffff14]'
    )

  return (
    <div className="min-h-screen flex flex-col md:flex-row">
      <aside className="md:w-56 shrink-0 border-b md:border-b-0 md:border-r border-[#ffffff1a] p-3 flex md:flex-col gap-1">
        <div className="text-[#ffffff] font-bold px-3 py-2 hidden md:block">GR7 Atendimento</div>
        <NavLink to="/inbox" className={linkCls}>
          <MessageSquare size={18} /> Atendimento
        </NavLink>
        {isAdmin && (
          <NavLink to="/admin" className={linkCls}>
            <Settings size={18} /> Admin
          </NavLink>
        )}
        <button
          onClick={() => signOut()}
          className="mt-auto flex items-center gap-2 px-3 py-2 rounded text-[#ffffff] text-sm hover:bg-[#ffffff14]"
        >
          <LogOut size={18} /> Sair {usuario?.nome ? `(${usuario.nome})` : ''}
        </button>
      </aside>
      <main className="flex-1 min-w-0">{children}</main>
    </div>
  )
}
```

- [ ] **Step 3: Criar `src/pages/Inbox.tsx`** (placeholder)

```tsx
export function Inbox() {
  return (
    <div className="p-6 text-[#ffffff]">
      <h1 className="text-xl font-bold">Atendimento</h1>
      <p className="text-[#ffffffb3] mt-2">A inbox chega no Plano 4.</p>
    </div>
  )
}
```

- [ ] **Step 4: Criar `src/pages/Admin.tsx`** (placeholder + sub-abas)

```tsx
import { NavLink, Routes, Route, Navigate } from 'react-router-dom'
import { cn } from '../lib/utils'

const ABAS = [
  { slug: 'departamentos', label: 'Departamentos' },
  { slug: 'tags', label: 'Tags' },
  { slug: 'motivos', label: 'Motivos' },
  { slug: 'mensagens-rapidas', label: 'Mensagens rápidas' },
  { slug: 'horario', label: 'Horário de Funcionamento' },
  { slug: 'bot', label: 'Configurações BOT' },
  { slug: 'usuarios', label: 'Usuários' },
]

export function Admin() {
  return (
    <div className="p-6 text-[#ffffff]">
      <h1 className="text-xl font-bold mb-4">Administração</h1>
      <nav className="flex flex-wrap gap-2 mb-6">
        {ABAS.map((a) => (
          <NavLink
            key={a.slug}
            to={`/admin/${a.slug}`}
            className={({ isActive }) =>
              cn('px-3 py-1.5 rounded text-sm', isActive ? 'bg-[#ffffff26]' : 'bg-[#ffffff14]')
            }
          >
            {a.label}
          </NavLink>
        ))}
      </nav>
      <Routes>
        <Route index element={<Navigate to="departamentos" replace />} />
        {ABAS.map((a) => (
          <Route key={a.slug} path={a.slug} element={<p className="text-[#ffffffb3]">{a.label}: em construção (Plano 3).</p>} />
        ))}
      </Routes>
    </div>
  )
}
```

- [ ] **Step 5: Escrever o teste do Login** (renderiza os campos)

`src/pages/Login.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { Login } from './Login'
import { AuthProvider } from '../lib/auth'

describe('Login', () => {
  it('mostra os campos de e-mail e senha e o botão Entrar', () => {
    render(
      <AuthProvider>
        <MemoryRouter>
          <Login />
        </MemoryRouter>
      </AuthProvider>
    )
    expect(screen.getByPlaceholderText('E-mail')).toBeInTheDocument()
    expect(screen.getByPlaceholderText('Senha')).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /entrar/i })).toBeInTheDocument()
  })
})
```

- [ ] **Step 6: Rodar todos os testes + build**

Run: `npm test && npm run build`
Expected: todos os testes PASS; build conclui sem erro.

- [ ] **Step 7: Commit**

```bash
git add src/components/Layout.tsx src/pages/Login.tsx src/pages/Inbox.tsx src/pages/Admin.tsx src/pages/Login.test.tsx
git commit -m "feat: layout com sidebar e telas placeholder (login, inbox, admin)"
```

---

## Validação final do Plano 1

- [ ] `npm run dev`, abrir o navegador: sem sessão cai em `/login`; após login válido, entra na `/inbox`; menu Admin só aparece para usuário com permissão `slug === 'admin'`; `/admin` redireciona não-admin para `/inbox`.
- [ ] `npm test` verde, `npm run build` sem erro.

Ao concluir, seguir para o **Plano 2 (Banco)**.
